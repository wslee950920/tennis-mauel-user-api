apiVersion: v1
kind: Pod
metadata:
  name: JenkinsCI
spec:
  containers:
    - image: gradle:latest
      name: gradle
      command:
        - sleep
      args:
        - 99d

    - image: docker:latest
      name: docker
      command:
        - sleep
      args:
        - 99d
      volumeMounts:
        - mountPath: /var/run/docker.sock
          name: containerd-sock

  volumes:
    - name: containerd-sock
      hostPath:
        path: /run/k3s/containerd/containerd.sock
        type: Socket