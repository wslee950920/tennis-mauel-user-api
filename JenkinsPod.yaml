apiVersion: v1
kind: Pod
metadata:
  name: JenkinsCI
spec:
  containers:
    - image: gradle:latest
      name: gradle
      command:
        - sleep
      args:
        - 99d

    - image: docker:dind-rootless
      name: docker
      command:
        - sleep
      args:
        - 99d